/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/stn-eoc',
  assetPrefix: '/stn-eoc',
  output: 'standalone',
  trailingSlash: true,
  reactCompiler: true,
};

export default nextConfig;
