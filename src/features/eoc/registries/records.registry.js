import BaseTeamRecords from "../teams/shared/components/base-team-records";

export const defaultTeamRecords = BaseTeamRecords;
export const baseTeamRecordsRegistry = Object.freeze({});
export const eocTypeTeamRecordsRegistry = Object.freeze({});
